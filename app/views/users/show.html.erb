<div class="jumbotron p-3 p-md-5 text-white rounded bg-secondary">
  <div class="col-md-6 px-0">
    <h1 class="display-4 font-italic"><%=@user.first_name%> <%= @user.last_name%></h1>
    <h5 class="card-title">Il vit à <%=@user.city.name%></h5></h5>
    <p class="card-text">Il a <%=@user.age%> ans</p> 
    <p class="card-text"><%=@user.description%></p>
    <p class="card-text">Son email : <%=@user.email%></p>
  </div>
</div>